# Copy to .env and fill in values if you want to override defaults.
# docker-compose.yml already sets these — only needed for local dev outside Docker.

PG_HOST=localhost
PG_PORT=5432
PG_DB=bazaar
PG_USER=bazaar
PG_PASSWORD=bazaarpass

MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=product-images
MINIO_USE_SSL=false
